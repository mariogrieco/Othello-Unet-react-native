const {
 getOneBoard,
 validate,
 eat,
 blanca,
 negra,
 canMove,
 clearOption,
 printState,
 setIn
} = require('./index')

board = getOneBoard()
board = setIn(board, blanca, 4, 2);
board = setIn(board, blanca, 4, 3);
board = setIn(board, blanca, 4, 4);
board = setIn(board, blanca, 4, 5);
board = setIn(board, negra, 2, 4);
board = setIn(board, negra, 3,);
board = setIn(board, blanca, 2, 3);
board = eat(board, 1, 3, negra)
board = validate(board, negra, blanca)
printState(board)
